import { defineMessages } from 'react-intl';

const messages = defineMessages({
    repliesTitle: {
        id: 'be.sidebarReplies.title',
        description: 'Title for the preview replies sidebar',
        defaultMessage: 'Reply',
    },
});

export default messages;
